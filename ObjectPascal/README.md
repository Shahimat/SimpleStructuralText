## ObjectPascal parser SST

files:

1. `PascalParserSST.pas` - lexer and parser;
2. `PascalSST.pas` - ObjectPascal internal structure (AST performance);
3. `AProjExample.lpr` - example console program.